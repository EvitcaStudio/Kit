{
  "name": "kit-singleplayer-template",
  "version": "1.0.0",
  "author": "{{PROJECT_AUTHOR}}",
  "description": "",
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "build": "kit build -v -i ./src/resources -o ./dist && bun run bun-build.ts",
    "play": "bun run build && bun run bun-serve.ts"
  },
  "keywords": [],
  "dependencies": {
    "@evitcastudio/kit": "^2.0.0"
  },
  "devDependencies": {
    "@evitcastudio/copy-bun-plugin": "^1.0.4",
    "@types/bun": "latest"
  }
}